Why do we need a backend if JavaScript runs in the browser?

We need a backend because the browser cannot safely store private data, access databases directly, or handle secure server-side logic. The backend processes requests, stores data, and sends information to the frontend.
